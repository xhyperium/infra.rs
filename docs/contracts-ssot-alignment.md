# contracts SSOT 对齐

| 字段 | 值 |
|------|-----|
| package | `xhyper-contracts` / lib `contracts` |
| path | `crates/contracts` |
| Active Spec | `.agents/ssot/contracts/spec/spec.md` |
| 状态 | R4 15-trait 已落地；contract-testkit DEFER |

替换 `#43`/`#46`/`#53` 的 `xhyper-contracts` 草图。消费者：`observex` 实现 `Instrumentation`；`resiliencx` 消费；adapters venue trait 接入 DEFER。
