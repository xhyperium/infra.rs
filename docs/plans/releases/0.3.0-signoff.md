# 生产签核包 — workspace `0.3.0`（PLAN-CORE-PROD-002）

> **状态：SIGNED · GO-with-Accepts**  
> 模板 SSOT：[`.agents/rules/prod-signoff-TEMPLATE.md`](../../governance/prod-signoff-TEMPLATE.md)  
> 草稿归档：[`2026-07-21-signoff-DRAFT.md`](./2026-07-21-signoff-DRAFT.md)（已由本文件取代为权威签核）

---

## 元信息

| 字段 | 值 |
|------|----|
| 版本 / Tag | workspace `0.3.0`（`publish = false`；本签核**不**批准 crates.io 发布；可选 git tag `v0.3.0` 另议） |
| 关联 | PLAN-CORE-PROD-002 · beads `infra-asa` · PR #98 #120 #121 #124 #125 #127 #128 #138 #141 |
| 支持矩阵 | [`support-matrix.md`](../../governance/support-matrix.md)：Linux x86_64 + MSRV 1.85 |
| 证据基线 commit | `70544d0`（`main` · #145 后；签核会话 2026-07-21） |
| 签核日期 | 2026-07-21 |
| 签核人（Maintainer） | `@ZoneCNH`（黄博） |
| 授权说明 | 会话指令「执行签核」；结论 **GO-with-Accepts**（≡ 模板 *GO with follow-ups*） |

---

## 红线

```text
本文件为 L5 签核权威记录。
分层就绪 ≠ 五个 crate 整体 Production Ready 营销表述。
adapters mock ≠ 生产 DB / MQ / 交易所客户端。
```

---

## 已合入实现证据

| 波次 | PR | 合入摘要 |
|------|-----|----------|
| 基线 | [#98](https://github.com/xhyperium/infra.rs/pull/98) | 五核心 P0/P1 可机器验证子集 |
| W0 | [#120](https://github.com/xhyperium/infra.rs/pull/120) | 计划 + artifacts 冻结 |
| W1 | [#121](https://github.com/xhyperium/infra.rs/pull/121) | decimalx oracle / 边界 / panicking 门禁 / scheduled miri·mutants |
| W2 | [#124](https://github.com/xhyperium/infra.rs/pull/124) | canonical committed wire v1.1–v1.3 |
| W3 | [#128](https://github.com/xhyperium/infra.rs/pull/128) | contracts 语义文档 + fakes + venue override 门禁 |
| W4 | [#125](https://github.com/xhyperium/infra.rs/pull/125) | adapters **离线 mock** 验证入口 |
| W5 | [#127](https://github.com/xhyperium/infra.rs/pull/127) | support-matrix / public-api baselines / 签核模板 |
| 收尾 | [#138](https://github.com/xhyperium/infra.rs/pull/138) / [#141](https://github.com/xhyperium/infra.rs/pull/141) | 计划状态 + 验收勾选 + 审计 §12 |

DEFER 表：[`../artifacts/defer-disposition.md`](../artifacts/defer-disposition.md)

---

## L1 — 正确性与不变量

- [x] 核心 crate 测试在官方矩阵通过
- [x] decimalx：非法 scale 不可表示；checked 路径；oracle 见 `crates/types/decimal/tests/oracle_diff.rs`
- [x] 无「整体 Production Ready」错误对外表述（计划/报告/本签核均保持分层口径）

**证据（签核会话 · worktree `docs/0.3.0-prod-signoff` · commit `70544d0`）：**

```text
cargo test -p contracts -p decimalx -p canonical -p kernel -p testkit --all-targets
→ exit 0（含 testkit public_surface / manual_clock_* 等集成测）

cargo clippy -p contracts -p decimalx -p canonical -p kernel -p testkit --all-targets -- -D warnings
→ Finished dev profile · exit 0

CI（Rust）main success:
https://github.com/xhyperium/infra.rs/actions/runs/29819724845
https://github.com/xhyperium/infra.rs/actions/runs/29819021093
```

---

## L2 — API / SemVer

- [x] `node scripts/quality-gates/check-public-api.mjs` 通过
- [x] baselines：`docs/api-baselines/{kernel,testkit,decimalx,canonical,contracts}.txt`
- [x] 0.x / `publish = false` 策略已知

**证据：**

```text
public-api: kernel… OK
public-api: testkit… OK
public-api: decimalx… OK
public-api: canonical… OK
public-api: contracts… OK
public-api gate: OK (5 package(s), baselines match)
```

---

## L3 — 平台与工具链

- [x] 官方支持 = Linux x86_64 + MSRV 1.85（Accept 非 Linux）
- [x] MSRV / 主轨 CI 绿（见 L1 CI URL；合入历史含 #98 MSRV 1.85）
- [x] 仅声明 Linux x86_64 为官方支持（未静默扩大矩阵）

**证据：**

```text
.agents/rules/support-matrix.md — Official: Linux + x86_64 + rust-version 1.85
workspace Cargo.toml: rust-version = "1.85", version = "0.3.0"
```

---

## L4 — 安全与供应链

- [x] `cargo deny check`（advisories/bans/licenses/sources ok）
- [x] 无密钥进入版本库（签核抽查；`.env` / `.claude/*.local.json` 仍 ignore）
- [x] `node scripts/quality-gates/check-decimal-no-panicking-ops.mjs` OK

**证据：**

```text
cargo deny check
→ advisories ok, bans ok, licenses ok, sources ok
→ warning: Zlib license allowance unmatched（非阻断）

decimal panicking-ops gate: OK (33 files scanned, 0 hits)

node scripts/quality-gates/check-canonical-align.mjs
→ ALL CHECKS PASSED
```

---

## L5 — 运维与回滚

- [x] 回滚路径：各波次 squash merge 可 `git revert <squash-sha>`；分支删除后可从 PR 还原
- [x] DEFER Accept 清单已读（见下节）
- [x] 发布决策：保持 `publish = false`；本签核**不**构成 crates.io 发布批准
- [x] Signed-off-by / 结论 — 见签核区

**回滚指引（故障面优先）：**

| 故障面 | 优先 revert 候选 |
|--------|------------------|
| adapters mock / 验证入口 | #125 |
| contracts 语义 / venue gate | #128 |
| wire DTO | #124 |
| decimalx 证据链 | #121 |
| 治理门禁 / baseline | #127 |

---

## Accept 风险清单（签字知悉）

| 项 | 说明 | DEFER |
|----|------|-------|
| 真实后端 | adapters **mock ≠** 生产 DB/MQ/交易所客户端 | DEFER-1 Accept |
| 二期 trait | ObjectStore / TimeSeries / Analytics / PubSub 深度合同未全量 | DEFER-2 Accept |
| 非 Linux | 未官方支持 | DEFER-6 Accept |
| Venue override | **运行时**门禁为主，非 compile-fail lint | DEFER-8 Close 口径 |
| fuzz | proptest/对抗 serde 轻量入口；**无**完整 `cargo fuzz` 靶场 | DEFER-4 子集 |
| package stable | 未宣称 crates.io / Spec Goal Achieved | — |
| path-triggered workflow 噪声 | 部分专项 workflow 在 main 上可能显示 failure；**主证据**为五核心本地门禁 + `CI（Rust）` 绿 | — |

---

## 分层就绪声明（签核确认）

| crate | 声明层级 | 说明 |
|-------|----------|------|
| `decimalx` | **L1** | oracle / 边界 / panicking 门禁 |
| `canonical` | **L2**（committed 子集） | v1 + v1.1–v1.3 |
| `contracts` | **mock-L3**（首批 trait） | Fake/conformance + offline mock；真实后端 Accept |
| `kernel` | **L1 + L4** | loom/MSRV + 支持矩阵 |
| `testkit` | **L1**（测试支持） | **非**生产 runtime |

**整体 Production Ready：否**（分层就绪 + Accept 残留；禁止营销式整体 PR 表述）。

---

## 签核结论（Maintainer）

| 字段 | 值 |
|------|----|
| 结论 | **GO-with-Accepts**（≡ 模板 *GO with follow-ups*） |
| 姓名 / `@handle` | 黄博 / `@ZoneCNH` |
| 日期 | 2026-07-21 |
| 备注 | 五核心 W0–W5 已合入；L1/L2/mock-L3/L4 证据在 `70544d0` 复验通过；Accept 见上表；不发布 crates.io |

```text
Signed-off-by: 黄博 (@ZoneCNH)  2026-07-21
```

### Follow-ups（不阻塞本签核）

1. 真实后端 live feature（postgres/redis/kafka/交易所）
2. 二期 trait 深度合同
3. 完整 `cargo fuzz` 靶场
4. 非 Linux 升官方支持（须改 support-matrix + CI）
5. 若公开发布：独立 release 计划 + 改 `publish` + tag 流程

---

## 变更记录

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 初版：DEFER-7 闭合；GO-with-Accepts；证据基线 `70544d0` |
