# canonical wire goldens — v1.2

| 文件 | 类型 | 等级 |
|------|------|------|
| `tick_legacy.json` | `Tick` | Committed v1.2 |
| `trade_legacy.json` | `Trade` | Committed v1.2 |

- `ts` = Unix epoch 纳秒（CAN-TIME-001）。
- 变更须跑 `cargo test -p canonical`。
