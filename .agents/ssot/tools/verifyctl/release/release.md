# tools/verifyctl — Release

> **状态**：默认 **BLOCKED**  
> workspace member `verifyctl` `0.1.0` 的最小 CLI 已实现；生产 verifier release 仍 **BLOCKED**。
>
> schema/digest/check 完整闭锁、真实超时取消、远程 runner、签名证据链和受控发布证据未闭合；禁止宣称 package stable / Production Ready / ship。
