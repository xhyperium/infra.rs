<!-- ssot:trace=domain_macro.release.001 -->
# domain_macro 发布记录

当前无可发布实现。`macrox` 的 `publish = false` 与规格版本解耦；发布前必须明确内部/公开渠道、schema 保留窗口、N-1 迁移、artifact 身份、回滚版本和 smoke test。provider 的来源授权、许可和 source-to-kernel 映射必须在 provider release 单独证明，不能由 kernel release 代替。
