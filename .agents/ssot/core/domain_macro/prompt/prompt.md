<!-- ssot:trace=domain_macro.prompt.001 -->
# domain_macro Agent Prompt

先读取 `manifest.json`、Goal、Design、Spec、Matrix、Gate 和 Evidence。一次只处理一个任务 ID；不得在未批准 ADR 前选择精度、时间、唯一键或 wire 形态，不得删除 `DataPoint<T>`，不得将测试计划或文档示例写入落地证据。交付必须包含修改文件、条款 ID、测试命令、退出码和剩余风险。
