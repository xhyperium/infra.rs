# adapters/storage/kafka — Evidence（横切）

> 模块战役证据落盘处（≠ `crates/evidence` 生产库）。  
> 当前：**无战役 evidence 归档**（布局迁移创建）。  
> 有验证输出时按 `YYYY-MM-DD/` 建日目录，并维护 `residual-open.*`。

参见 `.agents/ssot/kernel/evidence/` 与 `.agents/ssot/AGENTS.md` §2.5。
