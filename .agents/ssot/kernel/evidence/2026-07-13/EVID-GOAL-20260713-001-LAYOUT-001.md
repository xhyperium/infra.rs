# EVID-GOAL-20260713-001-LAYOUT-001

| 字段 | 值 |
|------|-----|
| Evidence ID | `EVID-GOAL-20260713-001-LAYOUT-001` |
| Type | layout migration |
| Date | 2026-07-13 |
| Source Goal | `GOAL-20260713-001` |

## What

将 `.agents/ssot/kernel/*.md` 扁平制品迁移为 `.agents/ssot/kernel/<phase>/<phase>.md`，Code 仍仅在 `crates/kernel`。

## Validation

```bash
for p in goal spec design plan tasks prompt test review release retrospective; do
  test -f ".agents/ssot/kernel/$p/$p.md" || exit 1
done
test -d crates/kernel
test ! -f .agents/ssot/kernel/goal.md
```

## Result

PASS（迁移后路径检查）。
