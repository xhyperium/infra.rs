# archgate.json — 历史 monorepo 产物说明

> **历史 monorepo 记录**（infra.rs）：文中 archgate / `.architecture` 不构成本仓验收条件；本仓不移植 archgate。

本目录下的 `archgate.json` 是 **2026-07-14** monorepo 战役会话中
`cargo run -p archgate -- --json` 的命令输出快照（含 cargo 运行前缀行 + JSON）。

- **不**构成本仓（infra.rs）任何验收门禁
- **不**要求本仓生成或更新同名文件
- 保留目的：审计追溯与战役证据完整性；**禁止删除**
