# RELEASE-SCHEDULEX-003

状态：BLOCKED

候选内容：现有 registry + explicit tick 声明面的 fail-closed、deterministic maintenance。当前仍为 0.1.1 开发态；前序 PR 合并/rebase 后按仓库规则 bump 一次。

禁止发布或宣称：后台 scheduler、分布式调度、持久化恢复、完整 cron、package stable、Production Ready/L5。

解除条件：根 `AGENTS.md` 修正并 rebase；版本/lock/STATUS/API baseline 默认 CI 接线；contract-testkit；全仓门禁、独立复审/验证、PR CI 与人工审批全部通过。
