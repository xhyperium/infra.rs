<!-- ssot:trace=treasury.goal.001 -->
# treasury — 离线目标

当前 `draft`/`not_started`；目标只描述待授权方向，不是当前网络合同。

- 保留财政记录来源身份、期间、单位、精度、修订和缺失原因；
- 对坏数值、重复身份、未知字段和排序冲突返回稳定错误；
- 以脱敏 fixture、人工审查、回滚目标和 commit-matched evidence 作为唯一晋级依据。
