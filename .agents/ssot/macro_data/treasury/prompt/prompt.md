<!-- ssot:trace=treasury.prompt.001 -->
# treasury — 离线 Agent 提示词

当前 `draft`/`not_started`，只处理脱敏财政记录 fixture 的解析、身份、期间、单位、修订、缺失语义和稳定错误。

不得猜测端点、过滤、排序、认证、客户端、缓存、限流、重试、日志或再分发许可；不得访问外部服务。执行前读取 manifest、spec、gate 和 evidence，并记录条款 ID、命令、退出码和未决风险。
