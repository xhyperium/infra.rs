<!-- ssot:trace=treasury.design.001 -->
# treasury — 离线设计

当前 `draft`/`not_started`，只设计脱敏财政记录 fixture 的来源身份、期间、单位、精度、修订和缺失语义。

来源、端点、认证、过滤、排序、客户端、限流、缓存和再分发许可均为 `UNKNOWN`；获批前不采纳 ADR、不创建依赖、不设计请求流程或真实验收。
