<!-- ssot:trace=japan_cb.evidence.001 -->
# japan_cb — 验证证据

当前 `not_started`，不存在 crate、解析器或 provider 运行证据；本文件不提供幽灵 Cargo 包名、虚构版本或可执行命令。`SDMX/CSV` 只是待来源合同确认的候选输入形态，不是已核验事实。

## 晋级前必须收集

- 来源机构、代码表、文档版本、访问日期、授权/许可证据；
- 脱敏正向/缺失/重复/坏值/修订 fixture 及 SHA-256；
- 解析命令原文、未被管道掩盖的退出码、测试报告和 reviewer；
- source series/period/unit/missing/vintage 到 kernel 的字段级映射；
- schema/N-1、停止摄取、批次隔离、重放和回滚演练。

上述材料齐全前，`spec_status`、`implementation_status` 和 release 均保持未开始，不能把占位状态计为通过。
