<!-- ssot:trace=japan_cb.goal.001 -->
# japan_cb — 离线目标

当前 `draft`/`not_started`；目标只描述脱敏时间序列的离线解析，不承诺外部服务、统计目录或数据获取接口。

- 保留系列身份、期间、单位、语言、修订和缺失原因；
- 对脱敏 SDMX/CSV fixture 的合法、缺失、未知和坏输入返回稳定错误；
- 以来源合同、授权、人工审查、回滚目标和 commit-matched evidence 作为未来晋级条件。

统计代码、来源地址、协议、认证、客户端、缓存、限流、发布节奏和再分发许可均为 `UNKNOWN`，须另建核验任务。
