<!-- ssot:trace=japan_cb.design.001 -->
# japan_cb — 离线设计

当前 `draft`/`not_started`，只设计脱敏 SDMX/CSV fixture 的解析、系列身份、期间、单位、修订和缺失语义。

统计代码、来源地址、认证、客户端、缓存、重试、限流和再分发许可均为 `UNKNOWN`；不得把候选类型、路径或依赖当作当前 API。获批前不创建网络客户端或运行真实测试。
