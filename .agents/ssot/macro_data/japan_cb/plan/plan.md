<!-- ssot:trace=japan_cb.plan.001 -->
# japan_cb — 受限落地计划

当前 `implementation_status=not_started`，`planned_code_paths=[]`；统计代码、来源地址、协议、授权、缓存和再分发许可均为 `UNKNOWN`。

- [ ] 整理脱敏 SDMX/CSV fixture、缺字段、坏数值、重复身份和乱序时间；
- [ ] 定义系列身份、期间、单位、修订和缺失原因的纯函数校验；
- [ ] 运行 workspace、离线测试、编码和 SSOT 门禁并记录证据。

不得创建网络客户端、缓存、重试、限流、具体 URL 或真实 E2E。合同和授权获批后，另建 provider 任务并原子更新 manifest。
