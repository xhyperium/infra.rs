<!-- ssot:trace=uk_cb.prompt.001 -->
# uk_cb — 离线 Agent 提示词

当前 `draft`/`not_started`，只处理脱敏 JSON/CSV fixture 的系列身份、期间、单位、修订、缺失值和稳定错误。

不得猜测来源 URL、Series ID、认证、User-Agent、HTTP 客户端、重试、限流、缓存或再分发许可；不得访问外部服务。执行前读取 manifest、spec、gate 和 evidence，并记录条款 ID、命令、退出码和未决风险。
