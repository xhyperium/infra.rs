<!-- ssot:trace=uk_cb.design.001 -->
# uk_cb — 离线设计

当前 `draft`/`not_started`，只设计脱敏 JSON/CSV fixture 的系列身份、期间、单位、修订、缺失值和稳定错误。

来源地址、Series ID、认证、User-Agent、客户端、重试、限流、缓存和再分发许可均为 `UNKNOWN`；获批前不设计联网采集或具体 Cargo 路径。
