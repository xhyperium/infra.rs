<!-- ssot:trace=uk_cb.goal.001 -->
# uk_cb — 离线目标

当前 `draft`/`not_started`；目标只描述英国宏观观测的脱敏解析，来源产品覆盖、发布时效、访问方式和外部服务兼容性均不承诺。

- 保留系列身份、观测期间、单位、精度、修订和缺失原因；
- 对脱敏 JSON/CSV fixture 的合法、缺失、未知和坏输入返回稳定错误；
- 对 `domain_macro` 映射建立可重放的纯数据测试；
- 以来源合同、字段证据、授权、人工审查和 commit-matched evidence 作为未来晋级条件。

来源、Series ID、端点、格式、参数、认证、采集时效、限流、缓存和许可均为 `UNKNOWN`，须另建核验任务。
