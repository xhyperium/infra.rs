<!-- ssot:trace=ecb.gate.001 -->
# ecb 门禁

- `spec_status=verified` 前必须存在官方契约、fixture、解析测试和人工审查记录。
- 任何未知维度、未知单位、非有限值、身份冲突或哈希不匹配必须失败。
- 真实网络测试不可替代离线测试；无授权时必须不执行联网采集。
- `implementation_status` 只能由真实代码、测试和 evidence 共同晋级，不能由本文件勾选。
