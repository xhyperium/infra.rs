<!-- ssot:trace=yield_curve.release.001 -->
# yield_curve 发布记录

当前没有可发布实现。kernel 发布前必须提供版本化 wire schema、值对象/算法 fixture 与测试报告、算法变更说明、artifact 身份和上一版本回滚路径；官方来源、访问授权和再分发许可由具体 provider release 负责，不由 kernel 声称。规格 `verified` 不等于发布。
