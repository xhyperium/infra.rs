<!-- ssot:trace=yield_curve.gate.001 -->
# yield_curve 门禁

- 未经官方来源、访问日期、fixture 哈希和许可依据，不得把 provider 标为 `verified`。
- 未知期限、曲线类型、单位、非有限值和重复身份必须失败。
- 派生曲线必须保留算法版本和输入身份；禁止覆盖官方值。
- 空扫描、跳过测试、warning-only 证据和不存在代码路径均不得通过。
