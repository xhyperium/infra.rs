<!-- ssot:trace=jin10.test.001 -->
# jin10 — 离线测试策略

当前只运行脱敏消息 fixture 和本地纯数据桩；不读取认证材料、不访问外部服务、不进行联网验收。

- 覆盖事件、日历、行情消息的合法、缺字段、未知字段、坏数值、重复身份、乱序时间和缺失原因；
- 覆盖 secret sentinel 不进入 Debug、Display、Serialize、URL、错误、tracing 或原始响应；
- 覆盖拒绝、未知权限和输入冲突的稳定错误；
- 合同、授权、脱敏配置、人工审查和回滚证据齐全后，才能另提网络测试。
