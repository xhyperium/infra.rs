---
description: infra.rs 项目开发规则 — 代码复用、内部库优先、模块独立性
globs:
  - "crates/**/*.rs"
  - "tools/**/*.rs"
version: 1.0.0
alwaysApply: true
---

<!-- SSOT: .agents/rules/项目开发规则.md — 本文件为 Cursor 投影摘要，正文以 SSOT 为准 -->

# infra.rs 核心开发规则

## DEV-001 优先复用内部库

新增功能前，必须先检索内部 crate / 模块索引，已有实现可直接复用的禁止重新实现。

- 可复用的直接依赖，不得以「略有差异」为由重写
- 需扩展的优先向内部库提交 PR / 扩展接口
- 确无对应时注明检索结果与不可替代理由

例外：性能不满足量化场景（需 benchmark）、接口改造成本高于重写、库已废弃或不维护。

## DEV-002 代码复用规则

相同逻辑只实现一次。相似逻辑出现 ≥2 次，必须抽象为公共函数 / 宏 / trait。

- 复用优先级：内部公共库 > 项目内公共模块 > 业务模块私有实现
- 禁止通过复制粘贴绕过复用
- 公共接口变更需评估所有调用方影响

## DEV-003 独立复用模块规则

复用模块必须独立、可测试、可单独发布，禁止依赖具体业务。

- 零业务依赖：不得引用上层业务 crate / 模块
- 独立编译：可单独 cargo build / cargo test
- 接口明确：通过 pub API 暴露，内部实现对外不可见
- 版本管理：接口变更遵循语义化版本
- 禁止硬编码业务配置、禁止反向依赖调用方、禁止包含不可复用的临时逻辑

## 工程规范

- 报告文档统一保存至 docs/report/{YYYY-MM-DD}/
- 提交格式：type(scope): 中文简述（Conventional Commits）
- 通过 cargo fmt/cargo clippy/cargo test 门禁后再提交
