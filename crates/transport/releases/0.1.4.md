# transportx 0.1.4

日期：2026-07-23  
状态：内部候选；等待 PR CI、独立审查与人工审批

本版本在 `0.1.3` 安全维护面上继续收紧边界：URL Debug 仅保留安全 origin；兼容
`HttpClientPool::new` 对无效配置 fail-fast，`try_new` 保持可恢复错误；删除仅供测试的
隐藏 public hook；未收到 Close frame 的 WS 断连标记为非正常关闭。factory
error/unwind、锁中毒、超时与异常 EOF 均有确定性回归覆盖。

企业 PKI/mTLS、M3 与完整业务 live 仍为 NO-GO。验证入口见
`.agents/ssot/transport/spec/spec.md`。
