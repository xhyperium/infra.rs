# transportx 0.1.2

| 字段 | 值 |
|------|-----|
| 日期 | 2026-07-22 |
| 范围 | 内部 workspace（`publish = false`） |

## 变更摘要

- **修复**：`HttpClientPool::checkout_with` 在 `factory()` 返回 `Err` 时回滚 `checked_out`，避免 size=1 池永久耗尽。
- **测试**：`factory_err_releases_slot_so_pool_not_exhausted` 回归。

## 验证

```bash
cargo test -p transportx --lib
cargo clippy -p transportx --all-targets -- -D warnings
```
