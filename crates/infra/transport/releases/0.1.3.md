# transportx 0.1.3

日期：2026-07-23

状态：内部候选；未 commit/publish

安全维护内容：HTTP 流式限额、WS 解码前限额、URL Debug 脱敏、SNI fail-closed、RAII 池、RFC 9110 Retry-After。无数据迁移，直接消费者只同步 path version。

NO-GO：M3、企业 PKI/mTLS、WS 企业 TLS、公网与完整业务 live。最终验证退出码见 `.agents/ssot/infra/transport/evidence/README.md`。
