# bootstrap 0.3.3 第 3 轮候选记录

| 字段 | 值 |
|---|---|
| 日期 | 2026-07-23 |
| 版本 | `0.3.3` |
| 状态 | 候选已重冻；本地 reviewer 完成、verifier 技术/证据初验完成；GitHub CI / PR / 审批 / 合并 pending |
| publish | `false` |

## 变更摘要

- 保留四条成功 build 路径的 shutdown owner，并提供 signal-before-drain 的 graceful 入口。
- ownerless 且 signal 未预触发时 fail-closed，不执行 hook。
- drain mutex poison 映射为保留 source 的依赖不可用错误。
- BootstrapError、XError context、drain 自有错误与示例输出使用简体中文。
- DependencyUnavailable 顶层 Display / XError context 不内插任意 source 文本，`#[source]` 链保留。
- 纳入 main `ContractStoreSet` 固定 KV/EventBus typed 注入与组合回归。
- 最终测试：46 + 10 + 4 = 60 passed、1 ignored；root 串行覆盖率：exit 0；`963 / 963`，zeros 0，
  100.0000%。此前 `975 / 975` 与 `961 / 961` 为中间树历史。

## 诚实边界

同步 hook 仍可能阻塞或 panic；没有 async drain/cancel、生产关停 SLA 或完整应用运行时。
本记录没有 commit、tag、外部发布、发布者签名或校验和；这些字段须在实际发布动作发生后补充。
