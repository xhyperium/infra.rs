# schedulex 0.1.2

发布日期：2026-07-23（候选；合并与人工批准前不宣称已发布）

本候选固化确定性 JobRunner 契约：失败原子性、稳定排序、显式 tick 时间语义、取消/替换、FixedDelay/Cron 进度及 panic 隔离。公开 API baseline、外部 seam 测试、全分支覆盖与独立审查证据必须在最终主干重放后再次通过。

本版本不把 schedulex 扩展为异步 runtime、持久化 scheduler 或分布式调度器。
