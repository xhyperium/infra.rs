# resiliencx 0.1.2 第 3 轮候选记录

| 字段 | 值 |
|---|---|
| 日期 | 2026-07-23 |
| 版本 | `0.1.2` |
| 状态 | 候选已重冻；本地 reviewer 完成、verifier 技术/证据初验完成；GitHub CI / PR / 审批 / 合并 pending |
| publish | `false` |

## 变更摘要

- 新增 generic safety-aware Adapter budget 同步/异步入口；旧无 safety 入口明确为 unchecked compatibility。
- Redis 生产 client budget 路径按只读/幂等/不安全副作用分类；Postgres 仅提供显式 safety wrapper，
  当前 pool 没有 budget 自动接线。
- `RetrySafety` 进入真实 sync/async 安全入口，整次 deadline 覆盖 operation 与退避。
- caller seed 驱动真实 jitter；budget reservation 在退避取消时 RAII refund。
- retry attempt 观测一致，bulkhead poison 恢复后容量可复用。
- 当前三包 test / clippy / doc / fmt 与 spec / dependency gates 本地通过。
- 最终测试计数：resiliencx 84 passed；postgresx 52 passed + 6 ignored；redisx 51 passed + 8 ignored。
- 首次当前树 coverage 为 `1106 / 1116`、99.1039%；缺失行为测试补齐后，root 串行复验为
  `1156 / 1156`、zeros 0、100.0000%、退出码 0。
- 此前 `994 / 994` 是本轮 Adapter safety 补丁前基线，不能作为当前候选结论。
- 最新固定 review 又改变 unchecked async core 与 Redis 零 attempts 路由；`1156 / 1156` 因而成为
  本次修复前基线；root 最终串行重跑为 `1208 / 1208`、zeros 0、100.0000%、退出码 0。

## 诚实边界

deadline 不撤销外部副作用；预算、熔断、限流与舱壁均为单进程原语，不提供分布式协调。
本记录没有 commit、tag、外部发布、发布者签名或校验和；这些字段须在实际发布动作发生后补充。
