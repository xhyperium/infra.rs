# observex 0.1.2 第 3 轮候选记录

| 字段 | 值 |
|---|---|
| 日期 | 2026-07-23 |
| 版本 | `0.1.2` |
| 状态 | 候选已重冻；本地 reviewer 完成、verifier 技术/证据初验完成；GitHub CI / PR / 审批 / 合并 pending |
| publish | `false` |

## 变更摘要

- 统一 `op` sanitizer，并保持 128 UTF-8 字节边界。
- 有界进程内 exporter 提供一致统计、flush-and-close 与 poison 恢复。
- exporter 普通错误 / unwind panic 诊断可见，用户可见错误为简体中文并使用 `thiserror`。
- 失败调用涉及的事件记为 `unconfirmed`：交付状态未知且 wrapper 不重试，不宣称实际丢弃。
- 本轮新树 root 串行覆盖率：`942 / 942`、zeros 0、100.0000%、exit 0。

## 诚实边界

同步 exporter 实现方仍须快速返回；没有 OpenTelemetry API/SDK、OTLP、远端持久化或阻塞隔离。
本记录没有 commit、tag、外部发布、发布者签名或校验和；这些字段须在实际发布动作发生后补充。
