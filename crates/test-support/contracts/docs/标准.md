# contract-testkit 标准文档

**包名**：`contract-testkit` v0.1.1 | **角色**：T0 合约测试套件 | **层级**：T0 (dev-dep)

---

## 1. 需求标准

### 目标

contract-testkit 为 contracts 各 trait 提供 Fake 实现和一致性测试套件（SPEC-TESTKIT-002 §3.2）。

### 边界

- **包含**：各 trait 的 Fake 实现、一致性测试套件（per-trait conformance suite）
- **不包含**：生产实现、性能测试、网络通信
- **依赖**：contracts、kernel、async-trait、tokio 等

### 约束

- dev-dep 仅用于测试
- 所有 Fake 实现在内存中完成

---

## 2. 设计标准

```text
contract-testkit
├── fake/        — FakeKeyValueStore, FakeEventBus 等
├── conformance/ — per-trait 一致性测试套件
└── recording/   — 录制/Fake 结合的测试用具
```

---

## 3-17 简写

### 编码标准 — 遵循 contracts 规范

### 测试标准 — 一致性测试套件覆盖所有 trait 契约

### 性能标准 — 不适用（测试工具）

### 安全标准 — N/A（纯测试）

### 可靠性 — 内存操作，无持久化

### 风控 — N/A

### 回测 — N/A

### 上线 — N/A（dev-dep 不上线）

### 运维 — N/A

### 文档 — API.md + README

### 交付 — dev-dep 锁定

### 评分 — 4/5

### 验收 — 一致性套件全通过

---

**版本**：v1.0
