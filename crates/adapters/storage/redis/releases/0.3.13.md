# redisx 0.3.13

## 要点

- 能力缺口闭合：数据结构 / Streams / MULTI / 配置硬化 / readiness / eval_sha
- live integration 14 项 + e2e data CRUD（含 structures/streams）
- bench api_matrix 扩全 API 族

## 残余 OPEN / NO-GO

| 项 | 状态 |
|----|------|
| package stable | OPEN（禁止宣称） |
| Pub/Sub 重连/必达 | NO-GO |
| Cluster / Sentinel / TLS live | OPEN（无拓扑 env 时 soft-skip） |
| 跨模块 SelfValidator 框架 | OOS |

## 验证

```bash
cargo test -p redisx --lib --features pubsub
scripts/live/export-foundationx-env.sh --env dev -- \
  cargo test -p redisx --features pubsub --test integration_all_api --test e2e_klines_crud -- --ignored --test-threads=1
```
