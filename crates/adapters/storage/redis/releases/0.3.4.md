# redisx 0.3.4 未发布候选

| 字段 | 值 |
|---|---|
| 日期 | 2026-07-23 |
| 版本事实 | Cargo 当前 `0.3.4`；`0.3.3` 为 main 既有历史 |
| 测试 | 51 passed + 8 ignored；ignored live 项需要外部 Redis |
| 状态 | 候选曾冻结；治理修正后最终 SHA、reviewer/verifier、CI 与发布均 pending |

- budget 下 GET/EXISTS/PTTL/MGET 按 `ReadOnly`、无 TTL SET/MSET 按 `Idempotent` 安全重试。
- 相对 TTL SET、DEL、PEXPIRE 的多次尝试在 I/O 前拒绝；PUBLISH 不自动重试。
- `RedisOperation::Set` 因无法携带 TTL 参数保守保持 `AmbiguousWrite`，client 按参数细分。
- 真实 Cluster/Sentinel/TLS live 仍 OPEN；Pub/Sub 仍只支持 Standalone。

本记录没有 tag、发布制品、签名或校验和。
