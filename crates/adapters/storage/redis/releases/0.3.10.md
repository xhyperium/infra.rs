# redisx 0.3.10

## 范围

- `redisx::selfcheck`：LIB-SELFCHECK-SPEC §6.5 全量检查项（11）
- 级别 / 短路 / token 隔离 / catalog / 配置 skip

## 诚实边界

- 与 `tools/verifyctl` 无关
- 无跨模块调度器 / OTel metrics 导出
- 需真实 Redis 才能 Full 绿；离线 probe 覆盖短路

## 验证

```bash
cargo test -p redisx --lib --features pubsub
cargo clippy -p redisx --all-targets --features pubsub -- -D warnings
# 可选 live：
# cargo test -p redisx --test live_selfcheck -- --ignored
```
