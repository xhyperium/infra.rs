# redisx 0.3.11

## 范围

- live 集成：`integration_all_api`（公开 API 全量）
- E2E：`e2e_klines_crud`（Binance K 线 CSV CRUD）
- 基准：`api_matrix`

## 运行

```bash
scripts/live/export-foundationx-env.sh --env dev -- \
  cargo test -p redisx --features pubsub \
    --test integration_all_api --test e2e_klines_crud -- --ignored --test-threads=1

scripts/live/export-foundationx-env.sh --env dev -- \
  cargo bench -p redisx --bench api_matrix
```

## 诚实边界

- 凭据仅来自 ZoneCNH `secrets/env/dev.md` 注入，禁止入库
- pipeline 大批量应分块（E2E 默认 32）
- 未宣称 package stable
