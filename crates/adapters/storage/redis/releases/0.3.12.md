# redisx 0.3.12

## 范围

- E2E 无 `/home/workspace/data` 时 soft-skip（CI 安全）
- live：retry budget、result message stream、PubSubFacade
- selfcheck：cancel、JSON 报告、§6.5 catalog ID 对齐

## 验证

```bash
cargo test -p redisx --lib --features pubsub
scripts/live/export-foundationx-env.sh --env dev -- \
  cargo test -p redisx --features pubsub \
    --test integration_all_api --test e2e_klines_crud -- --ignored --test-threads=1
REDISX_DATA_ROOT=/nonexistent cargo test -p redisx --test e2e_klines_crud -- --ignored
```

## 残余 OPEN

Cluster/Sentinel/TLS live · package stable · 跨模块 SelfValidator · 行覆盖 100%
