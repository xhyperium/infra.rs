# redisx 0.3.8 未发布候选

| 字段 | 值 |
|------|-----|
| 版本 | `0.3.8` |
| 范围 | P0 硬化 + P2 起步（deadline / pipeline / lock） |

## 新增

- `with_call_deadline`：排队计入总预算
- `get_bytes` / `set_bytes`
- `pipeline_set` / `eval_script` / `lock_acquire|release|extend` + `RedisLock.fence`

## 边界

- Cluster/Sentinel/TLS live OPEN
- 锁非业务正确性保证
- package stable 禁止
