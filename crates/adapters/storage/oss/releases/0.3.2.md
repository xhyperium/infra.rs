# ossx 0.3.2

| 字段 | 值 |
|---|---|
| 日期 | 2026-07-23 |
| 范围 | 内部 workspace（`publish = false`） |

## 变更摘要

- 远程 HTTP fail-closed；对象、错误体、并发、缓冲、分片和重试次数具有硬上限。
- multipart 全流程共享单一总 deadline；ETag 校验并安全生成完成 XML。
- 取消或清理失败后将 UploadId 写入 1024 条有界 orphan registry，可显式 abort 补偿。
- dev OSS 随机 key put/get/delete live 通过，失败断言前先执行对象清理。

## 未承诺

STS、lifecycle、TB 流式对象、崩溃后 registry 持久化与 package stable 保持 OPEN。
