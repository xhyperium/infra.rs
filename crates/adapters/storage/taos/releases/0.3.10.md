# taosx 0.3.10

## Fixed

- `exec_sql_ws`：空响应不再伪造成功，返回 `Unavailable`
- integration：TMQ poll / free `exec_sql_ws` 去掉恒真断言
- gap register N-05：24h soak 标为 SUPERSEDED（短时框架证据，非墙钟 24h PASS）

## Added

### Benchmarks

- `critical_path`：10 项 critical-path latency + memory (VmRSS) benchmark
- `query_scale`：query_series latency scaling benchmark (0-10K rows)

### Examples

- `btc_eth_crud`：Binance BTC/ETH kline 完整 CRUD（CSV→TDengine→QUERY→UPDATE→DELETE）
- `import_all`：全量数据导入工具（64 CSV → 20M rows, 0 errors, 77K rows/s）

### Tests

- 新增 10 项 CRUD 离线单元测试（insert/query/delete/schema/price conversion）

### Quality

- clippy `-D warnings` PASS（0 warnings）
- 67 unit + 11 conformance = 78 offline tests PASS
- 12 live tests PASS（smoke/integration/selfcheck）
- semver-checks 0 issues
- 全量数据导入验证：73 tables, 24.5M rows
