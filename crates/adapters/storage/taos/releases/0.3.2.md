# taosx 0.3.2

| 字段 | 值 |
|---|---|
| 日期 | 2026-07-23 |
| 范围 | 内部 workspace（`publish = false`） |

## 变更摘要

- 远程明文、空认证和 URL 注入 fail-closed；REST 禁止 redirect。
- Decimal 使用 `NCHAR(64+)` 文本并拒绝存量 `DOUBLE` schema，连接时精度探测失败或不一致均 fail-closed。
- 响应、SQL/batch、query rows、in-flight 与 close drain 具有硬上限。
- 固定 digest 单节点 REST/NCHAR Decimal live 2/2 通过。

## 未承诺

Native SQL、WS 长会话认证、HA、自动幂等重试与 package stable 保持 OPEN。
