# taosx 0.3.5

- 新增 `BatchWriteReport` / `BatchWritePartialError`
- `write_batch_report` / `write_batch_chunked_report` / `write_batch_chunked_outcome`
- 多 chunk 中途失败时报告准确 `accepted`/`failed`（**不**自动重试）
- N-1 表面测试注入假密码验证 Debug 脱敏
- 幂等自动重试 / package stable / Native SQL / HA 仍 NO-GO
