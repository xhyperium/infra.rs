# taosx 0.3.8

## 摘要

- `taosx::selfcheck`：LIB-SELFCHECK-SPEC §6.7 全量检查项（9）
- Basic / ReadWrite / Full + 短路；`tmq_subscribe` 诚实 Skipped
- live：`cargo test -p taosx --test live_selfcheck -- --ignored`

## 验证

```bash
cargo test -p taosx
# cargo test -p taosx --test live_selfcheck -- --ignored
```

## 边界

- 非 verifyctl；非 package stable；TMQ / HA / Native SQL 仍 NO-GO
