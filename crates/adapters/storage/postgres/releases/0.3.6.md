# postgresx 0.3.6

| 字段 | 值 |
|------|-----|
| 日期 | 2026-07-23 |
| 类型 | foundation DoD 闭合（内部） |
| package stable | **未宣称** |

## 变更摘要

- live 覆盖扩展至 Repository / query_opt / begin / connect_from_env / resiliencx
- SQLSTATE 映射文档锚点（FK → Invalid）
- SSOT / 对齐文档同步；10 轮 draft 审查落盘
- 本轮证据：offline unit、dev live 9/9、deadline 固定镜像、bench 有界

## 验证

```bash
cargo test -p postgresx --all-targets
cargo test -p postgresx --test live_postgres -- --ignored
node scripts/postgres-deadline-conformance.mjs
cargo bench -p postgresx --bench query_hot_path
```

## 边界

- 远程 TLS live OPEN
- COPY / migrations / read-replica / package stable DEFER
