# postgresx 0.3.4 未发布候选

| 字段 | 值 |
|---|---|
| 日期 | 2026-07-23 |
| 版本事实 | Cargo 当前 `0.3.4`；`0.3.3` 为 main 既有历史 |
| 测试 | 52 passed + 6 ignored；ignored live 测试需要外部 PostgreSQL |
| 状态 | 候选曾冻结；治理修正后最终 SHA、reviewer/verifier、CI 与发布均 pending |

- 延续显式 `RetrySafety` wrapper 与 unchecked compatibility 的诚实边界。
- `PostgresPool` 仍没有 budget 字段或自动 SQL 重试接线。
- 远程 PostgreSQL 仍只允许 `sslmode=require`；HA、自定义 CA/mTLS、COPY、迁移均非当前声明。

本记录没有 tag、发布制品、签名或校验和。
