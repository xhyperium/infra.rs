# kafkax 0.3.5

## 摘要

生产发布测试矩阵：离线功能/安全、隔离集成可靠性、扩展 bench、故障重建 runner。

## 命令

```bash
cargo test -p kafkax --test prod_offline
node scripts/kafka-prod-matrix.mjs --fault-restart
cargo bench -p kafkax --bench hot_path -- --quick
```

## 非宣称

- group / rebalance / native EOS / HA / package stable
- 24h soak 默认 CI 门禁
