# kafkax 0.3.6

## 摘要

按 `.cargo/draft/verifyctl.md`（LIB-SELFCHECK-SPEC）§6.2 落地 **kafka 模块库内自验证**。

## 交付面

| 项 | 状态 |
|----|------|
| `kafka.basic.metadata` | shipped |
| `kafka.rw.produce_consume` | shipped（`_self_check_{token}`） |
| `kafka.full.topic_create_delete` | shipped（`ensure_topic` + `delete_topic`） |
| `kafka.full.key_partition_routing` | shipped（应用层稳定 hash + Record.key） |
| `kafka.full.ordering_headers` | shipped partial（顺序 ok；headers 公共面未导出） |
| `kafka.full.offset_commit` | shipped（应用层 `OffsetCommitStore`） |
| `kafka.full.group_lag` | Skipped NO-GO |
| `kafka.full.large_message` | shipped（可配置字节） |
| `kafka.full.isr_health` | Skipped NO-GO |

## 边界

- 不等于 `tools/verifyctl`
- 不宣称 package stable
