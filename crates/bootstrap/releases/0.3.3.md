# bootstrap 0.3.3 未发布候选

| 字段 | 值 |
|---|---|
| 日期 | 2026-07-23 |
| 版本 | `0.3.3` |
| 状态 | 候选已重冻；本地 reviewer 完成、verifier 技术/证据初验完成；GitHub CI 与发布 pending |
| publish | `false` |

- 吸收 main `ContractStoreSet` 的固定 KV/EventBus typed 注入，不引入动态 Service Locator。
- 保留 shutdown owner、ownerless fail-closed、同步 LIFO drain 与中文错误边界。
- DependencyUnavailable 顶层 Display / XError context 不内插任意下层 source 文本，`#[source]` 链保留。
- 最终本地测试为 46 + 10 + 4 = 60 passed、1 ignored；root 串行 coverage 为 `963 / 963`、100%。
  此前 `975 / 975` 与 `961 / 961` 为中间树历史。

本记录没有 tag、外部发布、签名或校验和。
