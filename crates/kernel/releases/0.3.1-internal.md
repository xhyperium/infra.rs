# kernel 0.3.1（内部）

- 日期：2026-07-23
- 状态：内部 workspace 候选，未声明 crates.io 发布
- 变更：`ShutdownSignal::wait_timeout` 改为返回 `Result<bool, WaitTimeoutError>`，不可表示的 deadline 明确返回 `DeadlineOverflow`。
- 兼容性：公开返回签名发生破坏性变化；workspace 消费者已同步。
